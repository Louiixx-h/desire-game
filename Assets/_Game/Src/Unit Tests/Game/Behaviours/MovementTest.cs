using NUnit.Framework;

public class MovementTest
{

    [SetUp]
    public void SetUp()
    {
        
    }

    [TearDown]
    public void TearDown()
    {
        
    }
    
    [Test]
    public void GIVE_movement_WHEN_call_tick_THEN_change_rigidbody_velocity()
    {
        // Arrange
        /*
        var sprite = GameObject.CreatePrimitive(PrimitiveType.Cube).GetComponent<SpriteRenderer>();
        var rigidbody = sprite.GetComponent<Rigidbody2D>();
        var movement = new Movement(sprite, 5.0f, rigidbody, 10.0f);
        rigidbody.velocity = new Vector2(1.0f, 0.0f);
        */
        // Act
        /*
        movement.tick();
        */
        // Assert
        /*
        Assert.AreNotEqual(new Vector2(1.0f, 0.0f), rigidbody.velocity);
        */
    }
}
